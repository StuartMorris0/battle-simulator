import { motion } from 'framer-motion';
import styled from 'styled-components';

const StyledWrapper = styled.div`
  height: 100vh;
`;

const GameWrapper = styled.div`
  display: flex;
  height: 80%;
`;

export { StyledWrapper, GameWrapper };
